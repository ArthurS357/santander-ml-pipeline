[pytest]
testpaths = tests src
python_files = test_*.py
markers =
    unit: testes unitários rápidos, sem I/O externo
    integration: testes que executam o pipeline real em ambiente efêmero
filterwarnings =
    ignore::DeprecationWarning
